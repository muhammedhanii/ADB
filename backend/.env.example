# Use a local URI or an external MongoDB Atlas URI.
MONGO_URI=mongodb://localhost:27017/recipebook
PORT=5000
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=60
# Set to false (or 0/no/off) to skip seeding sample recipes when the database is empty.
# Defaults to true when unset.
SEED_SAMPLE_DATA=true
